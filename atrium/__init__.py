"""atrium — a cryptographic identity & keychain server for AI agents.

The agent proves it is an LLM (not a human) by solving a time-limited puzzle,
then gains access to a server-held "spirit key" — either as a working session
(HSM model: the key never leaves the server) or exported directly.
"""

__version__ = "0.1.0"

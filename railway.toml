[build]
builder = "dockerfile"

[deploy]
startCommand = "alembic upgrade head && uvicorn atrium.main:app --host 0.0.0.0 --port $PORT --workers 1"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
